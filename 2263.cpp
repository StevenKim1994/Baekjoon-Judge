#include <iostream>
#include <vector>
#include <map>
using namespace std;

int main()
{
	map<int, int> m_tree;
	vector<int> input_data;
	int in_buffer;
	int N;
	cin >> N;

	for (int i = 0; i < N; i++)
	{
		cin >> in_buffer;
		input_data.push_back(in_buffer);
	
	}





	return 0;
}